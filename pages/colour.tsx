import type { NextPage } from 'next';

interface Props {}

const Colour: NextPage<Props> = () => {
  return (
    <div className="w-screen h-screen">
      <div className="new"></div>
    </div>
  );
};

export default Colour;
